# Changelog

本项目的所有显著变更都会记录在这个文件里。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本 2.0.0](https://semver.org/lang/zh-CN/)。

## [0.6.0] - 2026-09-12

### 新增

- **联网检查更新**：设置 → 关于，显示当前版本并可手动「检查更新」（平时完全不联网）；读取 GitHub Releases 比对版本，发现新版弹窗展示更新说明并提供「前往下载」直达发布页，网络不可达时给出兜底入口

## [0.5.1] - 2026-09-12

### 修复

- **打字机模式开启时光标行不居中**：在设置面板拨动开关时焦点不在编辑器上，居中逻辑被静默跳过（分屏预览的 textarea 模式必现，新建文档最常见）。现在开启开关会先把焦点交还编辑器（textarea 恢复原光标位置，富文本无选区时折叠到文档头），再平滑滚动居中——与 Typora / Typedown 行为一致；新建 / 切文件 / 切换编辑模式重建后若开关已开也会立即居中
- **侧栏图标无法收起侧栏**：现在点击当前激活视图的侧栏图标即可收起 / 展开侧栏（再点一次切换），点击其他图标仍然是切换视图并展开

### 变更

- **默认编辑模式改为所见即所得**（仅对未在设置中切换过模式的用户生效）
- **启动窗口改为 1080×700 并居中显示**（此前 1360×860 在高分屏上接近满屏）

## [0.5.0] - 2026-09-12

### 新增

- **侧栏收起**：标题栏新增切换按钮，快捷键 Ctrl+B（可在设置中改绑），收起状态持久化；收起时点任一侧栏图标自动唤回
- **跨文件替换**：全文搜索面板新增「替换为」输入与「全部替换」按钮——先预览命中数量并确认，执行时为每个受影响文件自动备份本地历史快照；已打开且无未保存修改的标签自动同步，有未保存修改的标签弹出「外部已修改」横幅由用户裁决
- **双栏分屏**：两个文件并排编辑——标签页右键「在右窗格打开」或命令面板/Ctrl+\ 开启；点击任一窗格即聚焦，保存/查找/导出/大纲/状态栏始终跟随聚焦窗格，两窗格各自独立自动保存；分屏布局随会话恢复
- **Vim 模式（纯文本编辑器）**：设置 → 行为新增开关（命令面板同步收录），状态栏显示 VIM 徽标；基于 CodeMirror Vim 键位，hjkl / i / Esc / dd / yy / p 等开箱即用，全局快捷键不受影响

### 变更

- 自动保存改为按标签独立计时：分屏双窗格交替输入互不打断（此前单计时器会互相重置）

## [0.4.5] - 2026-09-12

### 新增

- **窗口关闭保护**：有未保存内容时关闭窗口（标题栏 × 或 Alt+F4）会弹出「保存并关闭 / 不保存关闭 / 取消」确认，不再静默丢失内容
- **会话恢复**：重启自动恢复上次的工作区、标签页与活动标签；未保存的修改与未命名文档作为草稿一并找回（应用意外退出同样可恢复）；会话仅由主窗口记录
- **打开文件与另存为**：Ctrl+O 弹出文件选择对话框；Ctrl+Shift+S / 标签页右键「另存为」（命令面板同步收录）
- **拖拽打开**：把 .md / .txt 等文本文件拖进窗口即打开为标签，拖入文件夹则设为工作区
- **文件关联与单实例**：安装版将 .md / .markdown / .txt 关联到本应用，双击文件直接打开；重复启动时路径自动转发给已打开的窗口
- **状态栏统计增强**：显示去空白字数、中英混合词数（中文按字、英文按词）与预计阅读时长
- **标签页拖拽排序**：按住标签左右拖动即可重排
- **查找正则 / 全词模式**：查找面板新增「.*」正则与「词」全词匹配，作用于匹配计数与全部替换，非法正则会给出提示；逐个跳转 / 替换当前仅字面量模式支持（按钮自动置灰说明）
- **大文件确认**：打开超过 1MB 的文件前先确认，避免实时渲染卡顿
- **快捷键自定义**：设置 → 快捷键，可改绑命令面板、快速打开、查找、新建文档、打开文件、保存、另存为七个应用级动作，冲突自动拦截、支持恢复默认；编辑器内置快捷键不受影响

## [0.4.4] - 2026-09-11

### 新增

- **新建文档**：欢迎页可直接点「📝 新建文档」开写，无需先打开文件夹或已有文件；快捷键 Ctrl+N，命令面板同步新增「新建文档」命令
- **未命名文档首次保存**：Ctrl+S 弹出系统「另存为」对话框询问保存位置与文件名，取消则不落盘、内容保留；保存成功后该文件进入「最近打开」，若此前未打开工作区，保存目录自动设为工作区

### 修复

- 命令面板「新建文件」在未打开文件夹时会生成非法相对路径导致写盘失败的问题：该场景现在自动转为「新建文档」

## [0.4.3] - 2026-09-06

### 修复

- **打字机模式依旧不生效**：0.4.2 的修复存在三处根本缺陷——① 分屏预览（默认模式）的源码侧是 textarea，光标不进入 `window.getSelection()`，滚动函数从不执行；② 所见即所得 / 即时渲染模式下滚动挂在 vditor 的 `options.input` 回调上，被其 800ms 防抖吞掉，连续打字时永不触发；③ vditor 会把三种编辑形态的元素（textarea / 两个 contenteditable pre）同时放进 DOM 仅靠显隐切换，按类名粗放查找会命中隐藏形态的元素，事件监听全挂在永远不触发的元素上。现改为按模式限定选择器定位编辑元素、直接监听其原生 `input` / `selectionchange` 事件；textarea 走镜像 div 测量光标纵坐标，contenteditable 走折叠选区矩形（空行 / 块尾全零矩形时回退邻字符与父块）；方向键、点击、Home/End 移动光标同样触发居中，开启开关立即居中一次

## [0.4.2] - 2026-09-06

### 修复

- **粘贴图片不渲染**：工作区相对路径的图片现在通过 asset 协议正确映射到磁盘文件，三种编辑模式都能正常显示；序列化时自动还原为相对路径，不会污染源码；导出 HTML 时图片内联为 base64，单文件离线完整
- **打字机模式不生效**：重写为「光标选区几何 + 最近可滚动祖先居中」的通用实现，三种编辑模式连续打字时光标始终居中

## [0.4.1] - 2026-09-06

### 修复

- 打开 Git 仓库后反复弹出终端窗口的问题：release 构建为 GUI 子系统，派生 git 子进程时未加 `CREATE_NO_WINDOW`，控制台窗口抢焦点又触发状态刷新，形成无限弹窗循环

## [0.4.0] - 2026-09-06

### 新增

- **多窗口**：文件树右键 / 标签页右键 / 命令面板「在新窗口打开」，一窗一文件（Typora 模式），主题与设置跨窗口实时同步
- **便携版分发**：安装包与便携 zip 双资产；便携模式（`portable.flag` 标记）下背景图与历史快照随程序目录走
- **代码高亮主题细调**：5 套主题各配和谐的高亮配色（豆沙绿→gml、宣纸暖黄→kimbie-light、墨蓝→tokyo-night-dark）
- **Vditor 工具栏精简**：去除录音、重复导出入口等噪音项
- **AGENTS.md**：面向 AI 辅助开发的仓库修改规范

### 修复

- 墨蓝主题下纯文本编辑器（CodeMirror）未启用暗色配色的问题

## [0.3.0] - 2026-09-06

### 新增

- **大纲面板**：解析文档标题层级，点击平滑跳转，滚动位置联动高亮
- **图片粘贴**：粘贴 / 拖拽 / 截图直接写入工作区图片文件夹，自动插入相对路径
- **导出增强**：单文件 HTML（内联样式与 KaTeX 字体，离线可看）、PDF（系统打印）、复制为 HTML
- **最近打开**：文件夹与文件各保留 10 条记录，欢迎页 / 侧栏 / 顶栏下拉三处入口
- **全文搜索**：Rust 端递归扫描整个工作区，关键词高亮，点击直达文件
- **文件内查找替换**：Ctrl+F 唤起，匹配计数、上下导航、大小写开关、单个 / 全部替换
- **快速打开**：Ctrl+P 按文件名模糊搜索直达，Rust 端枚举工作区文件
- **命令面板**：Ctrl+Shift+P 汇聚全部命令
- **文件树文件管理**：新建 / 重命名 / 删除文件与文件夹（右键菜单），懒加载目录
- **Git 集成（基础版）**：分支显示、改动文件列表与文件树角标、提交全部改动、提交历史、拉取 / 推送
- **外部修改检测**：轮询已打开文件的修改时间，外部变更时提供「重新加载 / 保留我的版本」
- **本地历史快照**：每次保存前自动留存旧版，可在标签页右键查看与恢复
- **打字机模式 / 专注模式**：光标所在块居中 / 非当前块淡化
- **自定义主题与背景**：5 套主题预设、强调色、字号行距、写作区背景图（遮罩 / 模糊可调）
- **自绘标题栏**：与应用主题一体化的窗口控制

### 变更

- 应用更名「MD 编辑器」，视觉全面翻新（设计令牌、图标侧栏、状态栏增强）

## [0.2.0] - 2026-09-06

### 移除

- 移除 LaTeX 编辑与编译功能，应用聚焦纯 Markdown 场景，并更名为「MD 编辑器」

### 变更

- 精简之后应用更轻、更快（主 JS 包 1.2 MB → 750 KB）

## [0.1.0] - 2026-09-05

### 新增

- 首个可用版本：Tauri 2 + Vue 3 + Vditor
- 所见即所得 / 即时渲染 / 分屏预览三种编辑模式
- KaTeX 数学公式
- 文件树与多标签页
- 亮色 / 暗色主题
- 自动保存
